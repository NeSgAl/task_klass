#ifndef ENTER_H
#define ENTER_H

#include <stdio.h>
#include <stdlib.h>

float** read_matrix(int* m, int* n);

float** enter_matrix(int* m, int* n);

float** generate_matrix(int* m, int* n);

#endif // ENTER_H